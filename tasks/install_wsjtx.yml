# HamPi64 (github.com/DaiTengu/HamPi64) — 64-bit port of HamPi for Raspberry Pi OS Trixie / Pi 5.
# Copyright (C) 2020-2024 Dave Slotter, W3DJS (original HamPi); (C) 2026 Mike Miller, KD9QHQ (HamPi64 fork).
# SPDX-License-Identifier: GPL-3.0-or-later
#
# WSJT-X Improved (DG2YCB) — official arm64 .deb, replaces mainline WSJT-X.
# Version pinned via group_vars (wsjtx_improved_*).
#
- name: Install WSJT-X Improved
  hosts: hampi
  become: yes
  tags: wsjtx
  tasks:
    - name: Download WSJT-X Improved .deb
      when: install_wsjtx_improved | default(true) | bool
      get_url:
        url: "https://sourceforge.net/projects/wsjt-x-improved/files/WSJT-X_v{{ wsjtx_improved_version }}/Raspberry%20Pi/wsjtx-{{ wsjtx_improved_version }}_improved_{{ wsjtx_improved_variant }}_{{ wsjtx_improved_build }}_Rpi_{{ wsjtx_improved_os }}_arm64.deb/download"
        dest: "/tmp/wsjtx-improved.deb"
        mode: "0644"
      retries: 3
      delay: 10
      register: dl
      until: dl is succeeded

    - name: Install WSJT-X Improved (apt resolves deps; force-overwrite shares an icon with wsjtx-data, kept for jtdx/js8call)
      when: install_wsjtx_improved | default(true) | bool
      apt:
        deb: "/tmp/wsjtx-improved.deb"
        dpkg_options: force-confdef,force-confold,force-overwrite
